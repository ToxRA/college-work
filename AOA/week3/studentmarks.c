#include <stdio.h>
#define MAX_SIZE = 100

struct Student{
    char* name;
    int rollno;
    float marks;
};

int main(void) {
    int num;

    printf("Enter the number of students: ");
    scanf("%d", num);

    for (int i = 0; i < num; i++) {
        
    }

    return 0;
}